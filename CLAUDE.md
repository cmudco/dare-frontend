@AGENTS.md

## Claude Code

Project instructions live in `AGENTS.md`, imported above, so that Codex and other agents read the
same file. Add Claude Code-specific guidance below this line rather than in `AGENTS.md`.

Skills are in `.agents/skills/`; the entries under `.claude/skills/` are symlinks to them. See
`.agents/README.md`.
