export enum SenderType {
  PLAYER = 1,
  AI_ASSISTANT = 2,
}

export enum Provider {
  OPENAI = 'openai',
  CLAUDE = 'claude',
  GEMINI = 'gemini',
  LLAMA = 'llama',
}

export enum FeedbackType {
  LIKE = 'like',
  DISLIKE = 'dislike',
}
